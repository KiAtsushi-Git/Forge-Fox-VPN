[workspace]
members = ["src-tauri"]
resolver = "2"

# Профили обязаны жить в корне workspace — в нерутовом пакете cargo их
# игнорирует (с предупреждением), и релиз собирался бы без lto.
[profile.release]
opt-level     = 3
lto           = true
codegen-units = 1
strip         = true
